import { useParams } from "react-router"

const Project = (props) => {

    return(
        <div>
            <h1>{props.name}</h1>
            Project show Page, showing just 1 project
        </div>
    )
}
 
export default Project